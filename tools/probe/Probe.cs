namespace Qvuln.Unlisted.Probe
{
    /// <summary>
    /// Placeholder. This package exists only so the solution references a
    /// package identity that Qodana's security metadata feed cannot resolve.
    /// </summary>
    /// <remarks>
    /// Block-scoped namespace on purpose: netstandard2.0 defaults to C# 7.3,
    /// which has no file-scoped namespaces.
    /// </remarks>
    public static class Probe
    {
        public static string Name
        {
            get { return "Qvuln.Unlisted.Probe"; }
        }
    }
}
